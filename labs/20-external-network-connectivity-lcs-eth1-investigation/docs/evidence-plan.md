# Evidence Plan

Only sanitized evidence may be published.

Replace real values with:
<HOST_IP>
<ZOS_IP>
<GATEWAY_IP>
<HOST_MAC>
<HOST_ADAPTER>
<NETWORK_PREFIX>
<DEVICE_ADDRESS>

Do not publish original unredacted screenshots.
