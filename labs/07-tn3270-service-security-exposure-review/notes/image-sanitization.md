# Image sanitization notes

The original screenshots contained sample or system-specific IP addresses, hostnames, user-like identifiers and keyring-like names. These values were redacted before publication.

Sanitized placeholders used conceptually:

```text
<IP_ADDRESS>
<HOSTNAME>
<USER_ID>
<KEYRING_NAME>
```

The original DOCX should not be published directly.
