# Evidence

Do not upload raw screenshots until sensitive values are masked.

Mask at least:

- z/OS IP address
- default gateway
- local subnet
- hostnames
- local paths
- device numbers if desired
- non-generic user IDs

The original working evidence was collected in a local document and should be sanitized before publication.
