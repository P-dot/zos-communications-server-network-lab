# Image sanitization notes

The source evidence came from `lab11(1).docx`.

The `/etc/hosts` output contained a concrete IP address and host names. These values were replaced with placeholders in the published screenshot:

```text
<ZOS_IP> <HOSTNAME> <ALIAS>
```

No original DOCX or raw host mapping output should be committed to GitHub.
