# Image sanitization

The captured screenshot already used the placeholder `<ZOS_IP>` instead of the real z/OS IP address.

No additional sensitive IP address, gateway, subnet, hostname or credential is published in this lab.
