# Evidence index

| File | Description |
|---|---|
| `screenshots/01_powershell_external_reachability_placeholder_attempt.png` | PowerShell `Test-NetConnection` execution against `<ZOS_IP>` placeholder. The result shows name resolution failure, so it documents the procedure but not real reachability. |
