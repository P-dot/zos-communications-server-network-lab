# Commands

The following commands were reviewed or used as the basis of the lab evidence.

```text
RACDCERT CERTAUTH LIST
RACDCERT ID(TCPIP) LIST
RACDCERT ID(TCPIP) LISTRING(*)
RACDCERT ID(FTPD) LIST
RACDCERT ID(FTPD) LISTRING(*)
RACDCERT ID(WEBSERV) LIST
RACDCERT ID(WEBSERV) LISTRING(*)
RACDCERT ID(START2) LIST
RACDCERT ID(START2) LISTRING(*)
```

## Commands intentionally not used

```text
RACDCERT ADD
RACDCERT GENCERT
RACDCERT DELETE
RACDCERT CONNECT
RACDCERT REMOVE
RACDCERT ALTER
SETROPTS RACLIST REFRESH
```
